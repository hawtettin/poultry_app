services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: poultry
      POSTGRES_USER: poultry
      POSTGRES_PASSWORD: poultry
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
